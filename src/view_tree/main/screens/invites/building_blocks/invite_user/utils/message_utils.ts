export function createMessage(firstName: string, inviteCode: string) {
    return `Hey ${firstName}! I have an invite to Digitari and want you to join! Here's the link. https://playdigitari.com/app\n\nOnce you have the app, use the invite code ${inviteCode} to unlock your account!`;
}
